>[!NOTE]
> VM NC AiO command logs



```bash
sudo apt update && sudo apt upgrade -y
curl
sudo su
sudo usermod -aG docker $USER
newgroup docker
sudo newgroup docker
ll /dev/dri
sudo apt install cifs-utils
sudo apt install -y qemu-guest-agent
sudo systemctl enable qemu-guest-agent
sudo apt install p7zip nano wget -y
sudo apt install -y nfs-kernel-server samba
sudo mkdir -p /mnt/nc_data
df -ht /mnt/nc_data
df
df -hT /mnt
df -hT /mnt/nc_data
ls
pwd
mkdir ./nextcloud-aio
ls
cd nextcloud-aio/
nano compose.yaml
su root
sudo su
pwd
cd ./nextcloud-aio/
ls
nano compose.yaml
docker compose up -d
nano compose.yaml
sudo poweroff

clear
cat /etc/fstab
sudo nano /etc/fstab
sudo systemctl daemon-reload
sudo mount -a

nano /home/glenn/.nascredentials
chmod 600 /home/glenn/.nascredentials
sudo nano /etc/fstab
sudo systemctl daemon-reload
sudo mount -a
df -h
ls -lah /mnt/nc_data
sudo reboot

docker ps
docker compose down
pwd
ls
cd ./nextcloud-aio/
docker compose down
docker ps
exit

pwd
sudo docker compose up -d
docker ps
docker exec --user www-data nextcloud-aio-nextcloud php occ config:system:set default_phone_region --value=US
ls -ld /mnt/nc_data
df -h
curl -s checkip.amazonaws.com
docker exec --user www-data nextcloud-aio-nextcloud php occ db:add-missing-indices
cd /mnt
ls
cd nc_data
cd nas_linkstation/

cd home
ls
cd glenn
id
ll
Now Install Immich
mkdir ./immich-app
cd ./immich-app/
ls
wget -O docker-compose.yml https://github.com/immich-app/immich/releases/latest/download/docker-compose.yml
ls
ll
wget -O .env https://github.com/immich-app/immich/releases/latest/download/example.env
nano .env
ls -la /mnt/nc_data
sudo ls -la /mnt/nc_data
docker compose up -d
df -ah
poweroff

history > history1
```
